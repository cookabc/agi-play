{
  "name": "wave",
  "version": "0.0.1",
  "description": "",
  "private": true,
  "scripts": {
    "dev": "parcel ./src/index.html --open",
    "clean": "rimraf ../public/wave && rimraf .parcel-cache dist",
    "build": "parcel build --no-scope-hoist --no-source-maps ./src/index.html --public-url /wave",
    "deploy": "npm run clean && npm run build && cp -r dist/ ../public/wave/"
  },
  "dependencies": {
    "echarts": "^5.4.3",
    "recorder-core": "^1.2.23070100"
  },
  "devDependencies": {
    "@parcel/transformer-less": "^2.10.3",
    "buffer": "^6.0.3",
    "parcel": "^2.10.3",
    "postcss": "^8.4.31",
    "process": "^0.11.10",
    "rimraf": "^5.0.5",
    "tailwindcss": "^3.3.5",
    "util": "^0.12.5"
  }
}
