{
  "name": "face",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "dev": "parcel ./src/index.html --open",
    "clean": "rimraf ../public/face && rimraf .parcel-cache dist",
    "build": "parcel build --no-scope-hoist --no-source-maps ./src/index.html --public-url /face",
    "deploy": "npm run clean && npm run build && cp -r dist/ ../public/face/"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "buffer": "^6.0.3",
    "parcel": "^2.10.0",
    "parcel-plugin-static-files-copy": "^2.6.0",
    "parcel-reporter-static-files-copy": "^1.5.3",
    "postcss": "^8.4.31",
    "process": "^0.11.10",
    "rimraf": "^5.0.5",
    "tailwindcss": "^3.3.5",
    "util": "^0.12.5"
  },
  "dependencies": {
    "face-api.js": "^0.22.2"
  },
  "browserslist": "> 0.3%, last 2 versions, not dead"
}
