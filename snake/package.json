{
  "name": "snake",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "dev": "parcel ./src/index.html --open",
    "build": "rimraf dist && parcel build --no-scope-hoist --no-source-maps ./src/index.html --public-url /snake && cp -r dist ../public/snake/"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@parcel/transformer-less": "^2.10.0",
    "buffer": "^6.0.3",
    "parcel": "^2.10.0",
    "postcss": "^8.4.31",
    "process": "^0.11.10",
    "rimraf": "^5.0.5",
    "tailwindcss": "^3.3.5",
    "util": "^0.12.5"
  },
  "dependencies": {
    "@tensorflow-models/speech-commands": "^0.4.0",
    "@tensorflow/tfjs": "^1.3.1"
  },
  "browserslist": "> 0.3%, last 2 versions, not dead"
}
