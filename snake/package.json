{
  "name": "snake",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "dev": "parcel ./src/index.html --open",
    "clean": "rimraf ../public/snake && rimraf .parcel-cache dist",
    "build": "parcel build --no-scope-hoist --no-source-maps ./src/index.html --public-url /snake",
    "deploy": "npm run clean && npm run build && cp -r dist/ ../public/snake/"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@parcel/transformer-less": "^2.10.3",
    "buffer": "^6.0.3",
    "parcel": "^2.10.3",
    "postcss": "^8.4.31",
    "process": "^0.11.10",
    "rimraf": "^5.0.5",
    "tailwindcss": "^3.3.5",
    "util": "^0.12.5"
  },
  "dependencies": {
    "@tensorflow-models/speech-commands": "^0.4.0",
    "@tensorflow/tfjs": "^1.3.1"
  },
  "browserslist": "> 0.3%, last 2 versions, not dead"
}
